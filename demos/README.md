# Demo Folders

Each public demo has its own folder under `demos/`.

- `craftutopia-build/`: current full runtime demo.
- The other folders are reserved demo rooms linked from the main gallery.

Shared images, videos, styles, scripts, and editable logs stay in `assets/` and `data/` until a demo needs private assets.
